<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/default_text_color</item>
        <item name="android:fontFamily">@font/aldrich</item>
    </style>

    <style name="asteroid_item_accent">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="asteroid_item">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="asteroid_item.gray">
        <item name="android:textColor">@color/text_grey</item>
    </style>

<!--  Based on Rob's Answer https://stackoverflow.com/a/54751236  -->
    <style name="AlertDialogTheme" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="buttonBarPositiveButtonStyle">@style/Alert.Button</item>
    </style>

    <style name="Alert.Button" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>
</resources>
